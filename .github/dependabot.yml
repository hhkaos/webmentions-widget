version: 2
updates:
  # The package itself has no runtime dependencies; this only keeps the
  # workflow actions current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: chore
